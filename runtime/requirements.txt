# ──────────────────────────────────────────────────────────────────────────────
#  V.I.E.W — Python AI Runtime requirements
#  RTX 4050 Laptop GPU (Compute Capability 8.9 · CUDA 12.x)
#
#  Install order:
#   1. Create venv (see SETUP.md)
#   2. Install PyTorch with CUDA 12.1 first (separate command — see below)
#   3. pip install -r requirements.txt
#
#  PyTorch CUDA 12.1 install command (run BEFORE pip install -r):
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# ──────────────────────────────────────────────────────────────────────────────

# Web framework (FastAPI + ASGI server)
fastapi==0.111.0
uvicorn[standard]==0.30.1

# YOLO + CV
ultralytics>=8.2.0
opencv-python

# Async HTTP (no Redis — direct push to Node.js)
aiohttp==3.9.5

# Data / utils
numpy==1.26.4
pydantic==2.8.2
pydantic-settings==2.3.4
python-dotenv==1.0.1

# Shape math (virtual fence — future use)
shapely==2.0.5
